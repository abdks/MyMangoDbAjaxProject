@model List<MyMangoDbAjaxProject.Dal.Entities.MainHome>
@{
    Layout = null;
}
<div class="container-fluid bg-light my-6 mt-0" id="home">
    <div class="container">
            @foreach (var item in Model)
        {
            
   
            <div class="row g-5 align-items-center">
                <div class="col-lg-6 py-6 pb-0 pt-lg-0">
                    <h3 class="text-primary mb-3">Ben</h3>
                    <h1 class="display-3 mb-3">@item.NameSurname</h1>
                    <h2 class="typed-text-output d-inline"></h2>
                    <div class="typed-text d-none">@item.Unvan</div>
                 @*    <div class="d-flex align-items-center pt-5">
                        <a href="" class="btn btn-primary py-3 px-4 me-5">CV</a>
                    </div> *@
                </div>
                <div class="col-lg-6">
                    <img class="img-fluid" width="500px" height="150px" src="~/proman-1.0.0/img/1.png" alt="">
                </div>
            </div>
        }
    </div>
</div>
